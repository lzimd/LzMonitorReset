<Window  x:Class="MonitorReset.MainWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         mc:Ignorable="d"
         MinHeight="{DynamicResource WindowMinHeight}" 
         MinWidth="{DynamicResource WindowMinWidth}"
         Title="{DynamicResource WindowTitle}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*" />
            <ColumnDefinition Width="60*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="30" Height="50*" />
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <Canvas x:Name="canvas_main" Grid.ColumnSpan="2" Grid.Row="0" Grid.Column="0" />
        <ListBox x:Name="list_process" Grid.Row="1" Grid.Column="0" />
        <TextBox x:Name="text_info" Grid.Row="1" Grid.Column="1" IsReadOnly="True" />
    </Grid>
</Window>
